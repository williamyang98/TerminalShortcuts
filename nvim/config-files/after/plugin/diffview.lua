require("diffview").setup({
    use_icons = false,
    -- replace webdev icons with portable ascii
    icons = {
        folder_closed = "+",
        folder_open = "-",
    },
    signs = {
        fold_closed = "+",
        fold_open = "-",
        done = "o",
    },
})
